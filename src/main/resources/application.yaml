spring:
  application:
    name: gsco
  datasource:
    url: jdbc:postgresql://localhost:5432/gsco_db
    username: postgres
    password: Enderact@08
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: postgresql
    show-sql: true